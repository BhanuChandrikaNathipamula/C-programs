

#include <stdio.h>
#include <stdlib.h>
int main()
{
    int i;
    int upper=0,lower=0;
    char ch[100];
    printf("Enter the String:\n");
    gets(ch);
    for(i=0; ch[i]!=0; i++){
      if(ch[i]>='A' && ch[i]<='Z'){
    upper++;
}
     else if(ch[i]>='a' && ch[i]<='z'){
    lower++;
}
    }
printf(" count of uppercase letters: %d",upper);
printf(" \ncount of lowercase letters: %d",lower);

getch();
    return 0;
}

Output:

Enter the String:
Aditya@123
count of uppercase letters: 1
count of lowercase letters: 5
